<h3><strong>商品詳細</strong></h3>

<div class="image">
  <%= image_tag @item.get_image(50, 50) %>
</div>
<div>
  <p>
    商品名 <%= @item.name %>
  </p>
  <P>
    商品説明 <%= @item.explanation %>
  </P>
  <p>
    ジャンル <%= @item.genre.name %>
  </p>
  <p>
    税込価格（税抜価格）<%= @item.add_tax_price.to_s(:dalimited) %>(<%= @item.price_without_tax %>)円
  </p>
  <p>
    販売ステータス
    <% if @item.is_sale == true %>
      販売中
    <% else %>
      販売停止
    <% end %>
  </p>
  <%= link_to "編集する", edit_admin_item_path(@item) class: "btn btn-success" %>
</div>